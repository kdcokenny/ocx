# Frozen registry maintenance

This worker serves verified legacy snapshots. Do not migrate their contents to the new schema or change old artifact bytes. New catalog development belongs in `../kdco-registry/catalog/`. Run the worker build to verify snapshot hashes.
